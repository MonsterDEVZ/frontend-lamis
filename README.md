# LAMIS Frontend

Next.js frontend для платформы электронной коммерции LAMIS с полной системой аутентификации.

## 🚀 Технологии

- **Next.js 14+** - App Router, React 18+
- **TypeScript** - Строгая типизация
- **Tailwind CSS** - Utility-first CSS
- **Zustand** - State management с persist
- **React Hook Form** - Управление формами
- **Zod** - Валидация схем
- **Framer Motion** - Анимации

## 📋 Требования

- Node.js 18+
- npm или yarn
- Backend API запущен на `http://127.0.0.1:8000`

## ⚙️ Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте переменные окружения

Файл `.env.local` уже создан с:

```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
```

Измените URL если backend работает на другом адресе.

## 🏃 Запуск

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно на: `http://localhost:3000`

### Production сборка

```bash
npm run build
npm start
```

## 📚 Структура проекта

```
frontend-lamis/
├── app/
│   ├── login/
│   │   └── page.tsx          # Страница входа
│   ├── register/
│   │   └── page.tsx          # Страница регистрации
│   ├── profile/
│   │   └── page.tsx          # Защищенная страница профиля
│   └── page.tsx              # Главная страница
├── components/
│   └── ui/
│       ├── Button.tsx        # Кнопка с loading state
│       ├── Card.tsx          # Card компоненты
│       └── Input.tsx         # Input с валидацией
├── services/
│   └── authService.ts        # API вызовы для auth
├── store/
│   └── authStore.ts          # Zustand store для auth
├── middleware.ts             # Защита роутов
└── .env.local                # Переменные окружения
```

## 🔐 Система аутентификации

### Страницы

- **`/register`** - Регистрация нового пользователя
- **`/login`** - Вход в систему
- **`/profile`** - Защищенная страница профиля

### Валидация форм

Используется **Zod** для валидации:

**Регистрация:**

- Email должен быть валидным
- Пароль минимум 8 символов
- Пароль должен содержать заглавную букву
- Пароль должен содержать цифру
- Подтверждение пароля должно совпадать

**Вход:**

- Email должен быть валидным
- Пароль обязателен

### State Management

**Zustand store** управляет:

- `isAuthenticated` - статус аутентификации
- `user` - данные пользователя
- `accessToken` - JWT access токен
- `refreshToken` - JWT refresh токен

Данные сохраняются в **localStorage** с помощью `persist` middleware.

### Защита роутов

**Middleware** (`middleware.ts`) автоматически:

1. Проверяет наличие токена перед доступом к защищенным роутам
2. Редиректит на `/login` если пользователь не аутентифицирован
3. Сохраняет redirect URL для возврата после входа

Защищенные роуты:

- `/profile/*`
- `/dashboard/*`

## 🎨 UI Components

### Button

```tsx
<Button variant="primary" isLoading={loading}>
  Отправить
</Button>
```

**Варианты:** `primary`, `secondary`, `outline`

### Input

```tsx
<Input label="Email" type="email" error={errors.email?.message} {...register('email')} />
```

### Card

```tsx
<Card>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
    <CardDescription>Описание</CardDescription>
  </CardHeader>
  <CardContent>Содержимое</CardContent>
</Card>
```

## 🔄 API Integration

Все API вызовы в `services/authService.ts`:

```typescript
// Регистрация
const user = await register({ email, password });

// Вход
const tokens = await login({ email, password });

// Обновление токена
const newTokens = await refreshToken(refreshToken);

// Получить текущего пользователя
const user = await getCurrentUser(accessToken);
```

## 🧪 Тестирование Flow

1. Запустите backend: `cd backend-lamis && uvicorn app.main:app --reload`
2. Запустите frontend: `npm run dev`
3. Откройте `http://localhost:3000/register`
4. Зарегистрируйте нового пользователя
5. Автоматический редирект на главную после регистрации
6. Перейдите на `/profile` - должны увидеть данные пользователя
7. Нажмите "Выйти"
8. Попробуйте зайти на `/profile` - должен редиректнуть на `/login`
9. Войдите снова - доступ к профилю восстановлен

## 🔒 Безопасность

- JWT токены хранятся в localStorage
- Middleware проверяет токены на серверной стороне
- Автоматическая валидация всех форм
- Защита от XSS через React
- CORS настроен только для разрешенных доменов

## 📝 Примеры использования

### Проверка аутентификации в компоненте

```tsx
'use client';

import { useAuthStore } from '@/store/authStore';

export default function MyComponent() {
  const { isAuthenticated, user } = useAuthStore();

  if (!isAuthenticated) {
    return <div>Пожалуйста, войдите</div>;
  }

  return <div>Привет, {user?.email}!</div>;
}
```

### Выход из системы

```tsx
const { clearAuth } = useAuthStore();

const handleLogout = () => {
  clearAuth();
  router.push('/login');
};
```

## 🚧 Roadmap

- [ ] Восстановление пароля
- [ ] Email верификация
- [ ] Социальная аутентификация (Google, Facebook)
- [ ] Двухфакторная аутентификация (2FA)
- [ ] Управление сессиями
- [ ] История входов

## 🤝 Интеграция с Backend

Frontend взаимодействует с backend API:

- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `POST /auth/refresh` - Обновление токена
- `GET /users/me` - Данные пользователя

Backend должен быть запущен на `NEXT_PUBLIC_API_URL`.

## 📄 Лицензия

Proprietary - LAMIS.KG
